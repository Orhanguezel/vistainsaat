// =============================================================
// FILE: src/app/(main)/admin/(admin)/contacts/page.tsx
// FINAL — Admin Contacts Page (App Router wrapper)
// =============================================================

import AdminContactsClient from './_components/admin-contacts-client';

export default function Page() {
  return <AdminContactsClient />;
}
